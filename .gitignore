# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# PyInstaller build artifacts
build/
dist/
*.spec

# Real game-data fixtures — copyrighted. Tests read them from repo root; copies may also
# sit under scratch/. Never commit them, wherever they land.
inputmappingprofiles*
wheeltunablesettingspc*
tests/gamedata/

# Local scratch: downloads, installer unpacks, throwaway experiments (all reproducible).
# download_pithouse.py now lives here, so it also writes the installer .exe here.
# Docs are not good enough to publish
scratch/
docs/
ROADMAP.*